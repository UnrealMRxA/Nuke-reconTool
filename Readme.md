# Nuke Recon Tool

A simple modular recon tool written in Python.  
Includes passive recon, active recon, enumeration, and OSINT modules.

## Features
- Modular structure (modules for passive, active, enum, osint)
- JSON output saving
- Easy to extend

## Install
See `INSTALL.md` for setup instructions.


